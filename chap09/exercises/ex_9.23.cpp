//练习 9.23：在本节第一个程序（第 309 页）中，若 c.size() 为 1，则 val、val2、val3 和 val4 的值回是什么？

/*
* 第 309 页的程序如下：
*/
// if (!c.empty()) {
//     auto val = *c.begin(), val2 = c.frony();
//     auto last = c.end();
//     auto val3 = *(--last);
//     auto val4 = c.back();
// }

/*
* 如果 c.size() 值为 1，则 val、val2、val3 和 val4 的值相同，都等于那一个元素的值。
*/
